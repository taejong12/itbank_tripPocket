<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.tripawards">


<select id="select" parameterType="TripAwardsDTO" resultType="TripAwardsDTO">
 SELECT * FROM trip_awards WHERE ...
</select>

<select id="select" parameterType="TripAwardsDTO" resultType="TripAwardsDTO">
    SELECT * FROM trip_awards WHERE ...  -- 조건 작성
</select>


</mapper>

